# 줄넘기 대회 관리 앱 전면 리팩토링 PRD

> **버전**: 2.0
> **작성일**: 2025-11-27
> **상태**: 계획 단계

---

## 1. 개요

### 1.1 프로젝트 배경
현재 줄넘기 대회 관리 앱은 기능이 점진적으로 추가되면서 데이터 구조가 복잡해졌습니다. 특히 `gradeConfig`의 `dateEvents`, `customEventsByDate` 등 중첩된 구조와 연습/대회 모드의 분리로 인해 유지보수가 어렵고, 학생 성장 추적이 비효율적입니다.

### 1.2 리팩토링 목표
1. **통합 워크플로우**: 연습/대회 모드를 게임 단위의 속성으로 통합
2. **학생 중심 데이터**: 학생별 기록 추적으로 성장 분석 최적화
3. **단순한 UI/UX**: 직관적인 네비게이션과 워크플로우

---

## 2. 사전 작업: 데이터 백업

### 2.1 백업 스크립트 (브라우저 콘솔 실행용)

```javascript
/**
 * 전체 데이터 백업 함수
 * 브라우저 콘솔에서 실행: backupAllData()
 */
async function backupAllData() {
  const userId = localStorage.getItem('jr_user_id');
  const competitionId = localStorage.getItem('jr_competition_id');

  if (!userId || !competitionId) {
    console.error('❌ 로그인 후 실행하세요');
    return;
  }

  console.log('📦 데이터 백업 시작...');
  console.log('👤 userId:', userId);
  console.log('🏆 competitionId:', competitionId);

  const backup = {
    exportDate: new Date().toISOString(),
    userId,
    competitionId,
    events: [],
    classes: [],
    gradeConfigs: []
  };

  try {
    // 1. Events 백업
    const eventsQuery = firebase.firestore()
      .collection('users').doc(userId)
      .collection('events')
      .where('competitionId', '==', competitionId);
    const eventsSnap = await eventsQuery.get();
    backup.events = eventsSnap.docs.map(d => ({ id: d.id, ...d.data() }));
    console.log(`✅ Events: ${backup.events.length}개`);

    // 2. Classes 백업 (학생 + 기록 포함)
    const classesQuery = firebase.firestore()
      .collection('users').doc(userId)
      .collection('classes')
      .where('competitionId', '==', competitionId);
    const classesSnap = await classesQuery.get();
    backup.classes = classesSnap.docs.map(d => ({ id: d.id, ...d.data() }));
    console.log(`✅ Classes: ${backup.classes.length}개`);

    // 3. GradeConfigs 백업
    const configsSnap = await firebase.firestore()
      .collection('users').doc(userId)
      .collection('gradeConfigs').get();
    backup.gradeConfigs = configsSnap.docs
      .filter(d => d.id.startsWith(competitionId))
      .map(d => ({ id: d.id, ...d.data() }));
    console.log(`✅ GradeConfigs: ${backup.gradeConfigs.length}개`);

    // 4. JSON 다운로드
    const blob = new Blob([JSON.stringify(backup, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `jumprope_backup_${new Date().toISOString().slice(0,10)}.json`;
    a.click();
    URL.revokeObjectURL(url);

    console.log('🎉 백업 완료! 다운로드 확인하세요.');
    return backup;
  } catch (error) {
    console.error('❌ 백업 실패:', error);
    throw error;
  }
}

// 전역 노출
window.backupAllData = backupAllData;
```

### 2.2 백업할 데이터 구조
| 컬렉션 | 경로 | 내용 |
|--------|------|------|
| events | `/users/{userId}/events` | 종목 목록 (16개 기본 종목) |
| classes | `/users/{userId}/classes` | 학급 + 학생 + results |
| gradeConfigs | `/users/{userId}/gradeConfigs` | 학년별 설정 + dateEvents |

### 2.3 삭제할 파일 (구 계획서/임시 파일)

**구 계획서** (이미 구현 완료):
- `CACHE_FIX_GUIDE.md`
- `DRAG_DROP_COPY_PLAN.md`
- `FIREBASE_INTEGRATION_PLAN.md`
- `FULLSCREEN_PWA_PLAN.md`
- `PARTICIPANT_SELECTION_PLAN.md`
- `PWA_구현_계획.md`
- `STUDENT_RECORD_PLAN.md`
- `TEAM_MANAGEMENT_PLAN.md`
- `TEST_CHECKLIST.md`
- `TROUBLESHOOTING_IPAD_VIEWPORT.md`
- `TROUBLESHOOTING.md`

**임시/백업 파일**:
- `firestore.rules.backup`
- `firestore.rules.temp`
- `add-authorized-domain.mjs`
- `auth_export.json`
- `pwa-썸네일_수정 (512*512).png`

**구 마이그레이션 스크립트**:
- `scripts/migrateToUserIdStructure.ts`

### 2.4 유지할 파일
- `README.md` - 프로젝트 설명 (리팩토링 후 업데이트)
- `firestore.rules` - 보안 규칙
- `firestore.indexes.json` - 인덱스 설정

---

## 3. 현재 데이터 구조 분석

### 3.1 기존 Firestore 구조
```
users/{userId}/
  ├── events/{eventId}              # 종목 마스터 (16개 기본)
  │   └── { id, name, type, defaultTimeLimit, defaultMaxParticipants }
  │
  ├── classes/{classId}             # 학급 + 학생 + 기록
  │   └── {
  │         grade, name, competitionId,
  │         students: [{ id, name, number, personalBests }],
  │         results: { [eventId]: ClassResult }
  │       }
  │
  └── gradeConfigs/{compId_grade}   # 학년별 설정
      └── {
            grade, competitionId,
            events: { [eventId]: { selected, targetParticipants } },
            dateEvents: { [date]: { [eventId]: {...} } },
            customEventsByDate: { [date]: CompetitionEvent[] }
          }
```

### 3.2 문제점
| 문제 | 영향 |
|------|------|
| `classes.results`에 모든 기록 저장 | 학생별 성장 추적 시 모든 학급 조회 필요 |
| `gradeConfig.dateEvents` 중첩 구조 | 복잡한 데이터 접근, 버그 발생 가능성 |
| 연습/대회 모드 분리 | 동일 학생 기록이 분산 저장 |
| `practiceRecords` 별도 컬렉션 | 통합 조회 불가, 일관성 문제 |

---

## 4. 신규 데이터 구조 설계

### 4.1 학생 중심 Firestore 구조
```
users/{userId}/
  │
  ├── events/{eventId}              # 종목 마스터 (유지)
  │   └── { id, name, type, defaultTimeLimit, defaultMaxParticipants }
  │
  ├── classes/{classId}             # 학급 정보 (results 제거!)
  │   └── {
  │         grade, name, competitionId,
  │         students: [{
  │           id, name, number,
  │           accessCode: 'AB3K'    # 🆕 개인 페이지 접근 코드
  │         }]
  │       }
  │
  ├── games/{gameId}                # 게임 세션 (신규)
  │   └── {
  │         date: '2025-11-27',
  │         mode: 'practice' | 'competition',
  │         grade: 3,
  │         eventIds: ['event1', 'event2'],
  │         status: 'completed' | 'in_progress'
  │       }
  │
  ├── records/{recordId}            # 모든 기록 (핵심!)
  │   └── {
  │         studentId, studentName, classId,
  │         accessCode,              # 🆕 조회용 코드 복사
  │         gameId, eventId, eventName,
  │         score, date,
  │         mode: 'practice' | 'competition',
  │         teamId?, teamMembers?, teamScore?
  │       }
  │
  └── accessCodes/{code}            # 🆕 코드 → 학생 매핑 (빠른 조회용)
      └── {
            studentId, studentName,
            classId, className,
            grade, userId           # 교사 ID (records 조회용)
          }
```

### 4.2 Student 타입 변경
```typescript
// types.ts
export interface Student {
  id: string;
  name: string;
  number?: number;
  accessCode: string;  // 🆕 4자리 랜덤 코드 (예: "AB3K")
  // ... 기존 필드
}
```

### 4.3 변경 이점
| 항목 | 기존 | 신규 | 이점 |
|------|------|------|------|
| 학생 기록 조회 | 모든 classes 순회 | `where('studentId', '==', id)` | 단일 쿼리 |
| 연습/대회 통합 | 별도 컬렉션 | mode 필드 | 통합 관리 |
| 성장 추적 | 분산 데이터 조합 | 시간순 정렬 쿼리 | 즉시 차트 생성 |
| 종목별 통계 | 수동 집계 | `where('eventId', '==', id)` | 실시간 통계 |
| 학생 개인 페이지 | 불가능 | accessCode로 바로 조회 | 로그인 없이 접근 |

---

## 5. UI/UX 리팩토링

### 5.1 사이드바 변경
**제거**: 연습/대회 모드 토글 스위치

**유지**:
- 학년(1-6) 네비게이션
- 학급 관리 메뉴
- 설정 메뉴

### 5.2 탭 구조 재구성
```
기존: 경기 설정 | 경기 기록 입력 | 경기 결과 종합
           ↓
신규: 게임 설정 | 기록 입력 | 결과 종합 | 성장 추적
```

### 5.3 게임 설정 탭 (통합 워크플로우)
```
┌─────────────────────────────────────────────────┐
│ 📅 날짜: [2025-11-27]  🎮 모드: [대회 ▾]       │
├─────────────────────────────────────────────────┤
│ 종목 선택:                                       │
│ ☑️ 개인줄넘기  ☑️ 8자줄넘기  ☐ 짝줄넘기         │
├─────────────────────────────────────────────────┤
│ 참가자 설정: (종목별)                            │
│ [개인줄넘기] 1반: 6명 선택됨  [수정]            │
│ [8자줄넘기]  1반: 8명 선택됨  [수정]            │
├─────────────────────────────────────────────────┤
│                              [게임 시작]         │
└─────────────────────────────────────────────────┘
```

### 5.4 성장 추적 탭 (교사용 - 학급 전체 보기)
```
┌─────────────────────────────────────────────────┐
│ 학급: [3학년 1반 ▾]  종목: [전체 ▾]             │
├─────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐    │
│ │ 김철수 │ │ 이영희 │ │ 박민수 │ │ 최수진 │    │
│ │ 📈+15% │ │ 📈+8%  │ │ 📈+22% │ │ 📈+5%  │    │
│ │ 최고 52│ │ 최고 48│ │ 최고 61│ │ 최고 40│    │
│ │ 🔗AB3K │ │ 🔗XY7P │ │ 🔗QW2M │ │ 🔗JH9N │    │
│ └────────┘ └────────┘ └────────┘ └────────┘    │
├─────────────────────────────────────────────────┤
│ [코드 일괄 출력]  [QR코드 생성]                  │
└─────────────────────────────────────────────────┘
```

### 5.5 학생 개인 페이지 (학생/학부모용)

**접근 방식**:
- URL: `https://앱주소/student/{accessCode}`
- 로그인 없이 코드만으로 접근 가능
- 학생 등록 시 자동으로 4자리 랜덤 코드 생성

**코드 생성 규칙**:
```typescript
// 혼동하기 쉬운 문자 제외: 0, O, I, l, 1
const CHARS = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789';

function generateAccessCode(): string {
  let code = '';
  for (let i = 0; i < 4; i++) {
    code += CHARS[Math.floor(Math.random() * CHARS.length)];
  }
  return code; // 예: "AB3K", "XY7P"
}
```

**학생 개인 페이지 UI**:
```
┌─────────────────────────────────────────────────┐
│  🏃 김철수의 줄넘기 성장 기록                    │
│  3학년 1반                                       │
├─────────────────────────────────────────────────┤
│  [대회 기록]  [연습 기록]  ← 탭 전환             │
├─────────────────────────────────────────────────┤
│                                                  │
│  📊 개인줄넘기 성장 추이                         │
│  ┌───────────────────────────────────────────┐  │
│  │  60┤          ●(대회)                     │  │
│  │  50┤      ○       ○(연습)                │  │
│  │  40┤  ○      ●                            │  │
│  │  30┤●                                     │  │
│  │    └──────────────────────────────────    │  │
│  │     11/1  11/8  11/15  11/22  11/27       │  │
│  │     ● 대회  ○ 연습                        │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
├─────────────────────────────────────────────────┤
│  🏆 종목별 최고 기록                             │
│  ┌──────────────┬──────────┬──────────┐        │
│  │ 종목         │ 대회최고 │ 연습최고 │        │
│  ├──────────────┼──────────┼──────────┤        │
│  │ 개인줄넘기   │ 52회     │ 58회     │        │
│  │ 8자줄넘기    │ 45회     │ 50회     │        │
│  │ 짝줄넘기     │ 38회     │ 42회     │        │
│  └──────────────┴──────────┴──────────┘        │
│                                                  │
├─────────────────────────────────────────────────┤
│  📈 성장 요약                                    │
│  • 총 연습 횟수: 15회                            │
│  • 총 대회 참가: 3회                             │
│  • 평균 성장률: +18%                             │
│  • 가장 성장한 종목: 개인줄넘기 (+22%)           │
└─────────────────────────────────────────────────┘
```

**대회/연습 비교 차트**:
- 같은 차트에 두 가지 마커로 표시 (●대회, ○연습)
- 탭으로 대회만/연습만/전체 필터링
- 종목별 최고기록을 대회/연습 구분하여 표시

### 5.6 연습/대회 성장 추이 분석

**핵심 인사이트**: 연습과 대회는 다른 성격의 기록이므로 분리 분석 필요

**성장률 계산 공식**:
```typescript
interface GrowthAnalysis {
  // 연습 기록 분석
  practice: {
    records: number[];           // 모든 연습 기록 (시간순)
    average: number;             // 평균
    best: number;                // 최고 기록
    recentAvg: number;           // 최근 5회 평균
    previousAvg: number;         // 이전 5회 평균
    growthRate: number;          // (최근 - 이전) / 이전 * 100
    trend: 'up' | 'down' | 'stable';
  };

  // 대회 기록 분석
  competition: {
    records: number[];           // 모든 대회 기록 (시간순)
    average: number;
    best: number;
    recentAvg: number;
    previousAvg: number;
    growthRate: number;
    trend: 'up' | 'down' | 'stable';
  };

  // 비교 분석
  comparison: {
    practiceVsCompetition: number;  // 연습평균 vs 대회평균 차이
    competitionPressure: number;    // 대회 시 하락률 (연습최고 대비)
    consistency: number;            // 일관성 점수 (표준편차 기반)
  };
}
```

**학생 개인 페이지 - 성장 분석 UI**:
```
┌─────────────────────────────────────────────────┐
│  📊 성장 분석 (개인줄넘기)                       │
├──────────────────────┬──────────────────────────┤
│      연습 성장       │       대회 성장          │
├──────────────────────┼──────────────────────────┤
│  📈 성장률: +22%     │  📈 성장률: +15%         │
│  평균: 48회          │  평균: 42회              │
│  최고: 58회          │  최고: 52회              │
│  최근추세: 상승 ↑    │  최근추세: 상승 ↑        │
├──────────────────────┴──────────────────────────┤
│  💡 분석 인사이트                               │
│  • 연습 대비 대회 기록 -12% (긴장 영향)         │
│  • 꾸준한 성장세 유지 중                        │
│  • 최근 5회 평균이 전체 평균 대비 +18%          │
└─────────────────────────────────────────────────┘
```

**성장 추이 차트 (듀얼 라인)**:
```
┌───────────────────────────────────────────────────┐
│  점수                                             │
│  60┤                    ●─────●                  │
│    │               ○──○     연습(실선)          │
│  50┤          ●────────●                         │
│    │     ○────○         대회(점선)              │
│  40┤○────────●                                   │
│    │                                              │
│  30┤                                              │
│    └────┬────┬────┬────┬────┬────┬────→ 날짜    │
│        10/1 10/8 10/15 10/22 10/29 11/5          │
│                                                   │
│  ── 연습   ●● 대회                               │
└───────────────────────────────────────────────────┘
```

**탭별 표시 내용**:
| 탭 | 차트 내용 | 통계 |
|----|----------|------|
| 전체 | 연습+대회 듀얼 라인 | 전체 성장률, 평균 |
| 연습 | 연습 기록만 | 연습 성장률, 연습 최고 |
| 대회 | 대회 기록만 | 대회 성장률, 대회 최고 |

---

## 6. 마이그레이션 전략

### 6.1 마이그레이션 스크립트
```typescript
// utils/migrateToStudentRecords.ts

interface MigrationResult {
  recordsCreated: number;
  errors: string[];
}

async function migrateToStudentRecords(userId: string): Promise<MigrationResult> {
  const result: MigrationResult = { recordsCreated: 0, errors: [] };

  // 1. 모든 classes 조회
  const classes = await getAllClasses(userId);

  for (const cls of classes) {
    // 2. classes.results → records 변환
    for (const [eventId, classResult] of Object.entries(cls.results || {})) {

      // 개인전: studentScores
      if (classResult.studentScores) {
        for (const [studentId, score] of Object.entries(classResult.studentScores)) {
          const student = cls.students.find(s => s.id === studentId);

          await createRecord({
            studentId,
            studentName: student?.name || 'Unknown',
            classId: cls.id,
            eventId,
            score,
            date: classResult.date || '날짜 미지정',
            mode: 'competition'
          });
          result.recordsCreated++;
        }
      }

      // 단체전: teams
      if (classResult.teams) {
        for (const team of classResult.teams) {
          for (const memberId of team.memberIds) {
            const student = cls.students.find(s => s.id === memberId);

            await createRecord({
              studentId: memberId,
              studentName: student?.name || 'Unknown',
              classId: cls.id,
              eventId,
              score: team.score,
              date: classResult.date || '날짜 미지정',
              mode: 'competition',
              teamId: team.id,
              teamScore: team.score
            });
            result.recordsCreated++;
          }
        }
      }
    }
  }

  return result;
}
```

### 6.2 마이그레이션 순서
1. **백업**: `backupAllData()` 실행
2. **신규 컬렉션 생성**: `records` 컬렉션 구조 준비
3. **데이터 변환**: 마이그레이션 스크립트 실행
4. **검증**: 데이터 무결성 확인
5. **코드 전환**: 새 서비스 함수로 교체
6. **정리**: 기존 `classes.results` 필드 제거 (선택)

---

## 7. 구현 단계

### Phase 1: UI/UX 개선 (1주차)
| 작업 | 파일 | 내용 |
|------|------|------|
| 모드 토글 제거 | `Sidebar.tsx` | practiceMode 관련 UI 제거 |
| 탭 구조 변경 | `GradeView.tsx` | 4탭 구조로 재구성 |
| 상태 정리 | `App.tsx` | practiceMode 상태 제거 |
| 게임 설정 통합 | 신규 컴포넌트 | 통합 워크플로우 UI |

### Phase 2: 데이터 구조 (2주차)
| 작업 | 파일 | 내용 |
|------|------|------|
| 타입 정의 | `types.ts` | Game, Record 타입 추가 |
| 서비스 함수 | `services/firestore.ts` | 새 CRUD 함수 |
| 마이그레이션 | `utils/migration.ts` | 데이터 변환 스크립트 |
| 보안 규칙 | `firestore.rules` | records 컬렉션 규칙 추가 |

### Phase 3: 성장 추적 + 학생 개인 페이지 (3주차)
| 작업 | 파일 | 내용 |
|------|------|------|
| 코드 생성 | `lib/accessCodeGenerator.ts` | 4자리 랜덤 코드 생성 |
| 학생 등록 수정 | 학급 관리 컴포넌트 | 등록 시 자동 코드 부여 |
| 교사용 탭 | `StudentGrowthTab.tsx` | 학급별 학생 카드 목록 |
| 학생 카드 | `StudentGrowthCard.tsx` | 코드 + 성장률 표시 |
| 코드 출력 | `AccessCodePrintModal.tsx` | 일괄 출력/QR 생성 |
| 학생 페이지 | `pages/StudentPage.tsx` | /student/:code 라우팅 |
| 대회/연습 탭 | `RecordModeToggle.tsx` | 기록 모드 필터링 |
| 성장 차트 | `GrowthChart.tsx` | 대회●/연습○ 구분 차트 |
| 최고기록 표 | `BestRecordsTable.tsx` | 종목별 대회/연습 최고 |
| 통계 계산 | `lib/statsCalculator.ts` | 성장률, 평균 등 계산 |

---

## 8. 파일 목록

### 8.1 수정할 파일
1. `components/Sidebar.tsx` - 모드 토글 제거
2. `components/GradeView.tsx` - 탭 구조 재구성
3. `App.tsx` - practiceMode 상태 제거
4. `types.ts` - 새 타입 추가
5. `services/firestore.ts` - 새 서비스 함수
6. `firestore.rules` - records 컬렉션 규칙

### 8.2 새로 만들 파일

**게임 설정**:
- `components/GameSettingsTab.tsx` - 통합 게임 설정

**성장 추적 (교사용)**:
- `components/StudentGrowthTab.tsx` - 성장 추적 탭 컨테이너
- `components/StudentGrowthCard.tsx` - 학생 카드 (코드 표시)
- `components/AccessCodePrintModal.tsx` - 코드 일괄 출력 모달

**학생 개인 페이지 (학생/학부모용)**:
- `pages/StudentPage.tsx` - 학생 개인 페이지 (라우팅: /student/:code)
- `components/student/StudentHeader.tsx` - 학생 정보 헤더
- `components/student/RecordModeToggle.tsx` - 대회/연습 탭 전환
- `components/student/GrowthChart.tsx` - 성장 차트 (대회/연습 구분)
- `components/student/BestRecordsTable.tsx` - 종목별 최고기록 표
- `components/student/GrowthSummary.tsx` - 성장 요약 카드

**유틸리티**:
- `lib/accessCodeGenerator.ts` - 랜덤 코드 생성
- `lib/statsCalculator.ts` - 통계 계산 유틸
- `utils/migrateToStudentRecords.ts` - 마이그레이션

**라우팅**:
- 기존 `App.tsx`에 `/student/:code` 라우트 추가

### 8.3 삭제할 파일 (정리)
- 11개 구 계획서 MD 파일
- 5개 임시/백업 파일
- 1개 구 마이그레이션 스크립트

---

## 9. 롤백 계획

1. **백업 파일 보관**: `jumprope_backup_YYYY-MM-DD.json`
2. **복원 함수 준비**:
```javascript
async function restoreFromBackup(backupData) {
  // 1. events 복원
  // 2. classes 복원
  // 3. gradeConfigs 복원
}
```
3. **Git 브랜치 전략**:
   - `main`: 현재 안정 버전
   - `refactor/v2`: 리팩토링 작업 브랜치
   - 문제 발생 시 `main`으로 롤백

---

## 10. 성공 기준

- [ ] 연습/대회 모드 통합 워크플로우 완성
- [ ] 학생별 성장 추적 차트 표시 (대회/연습 구분)
- [ ] 학생 개인 페이지 접근 코드 자동 생성
- [ ] `/student/:code` 라우팅으로 개인 페이지 접근
- [ ] 대회/연습 기록 분리 표시 (탭 전환)
- [ ] 기존 데이터 100% 마이그레이션 완료
- [ ] 모든 기존 기능 정상 작동
- [ ] iPad/데스크톱/모바일 반응형 UI 유지

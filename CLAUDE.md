# CLAUDE.md - 줄넘기 대회 관리 앱

## 1. 목적

초등학교 줄넘기 대회/연습 기록을 관리하고, 학생 성장을 추적하는 PWA 앱입니다.

**핵심 목표**:
- 교사가 학년별 줄넘기 종목을 설정하고 기록 입력
- 학급별/학생별 대회 및 연습 기록 저장
- 학생 성장 추적 및 교사 주도 공유 (성장 차트, 기록표 등)
- iPad/태블릿에서 현장 기록 입력 최적화

**사용자**: 교사 (종목 설정, 참가자 선택, 기록 입력, 결과 종합, 성장 기록 공유)

---

## 2. 기술 스택

| 영역 | 기술 | 용도 |
|------|------|------|
| **프레임워크** | React 19 + TypeScript | SPA 프론트엔드 |
| **빌드 도구** | Vite 6 | 빠른 개발 서버 + 번들링 |
| **스타일링** | Tailwind CSS | 유틸리티 기반 CSS |
| **백엔드/DB** | Firebase Firestore | 실시간 NoSQL 데이터베이스 |
| **인증** | Firebase Auth (익명) | 사용자 식별 |
| **호스팅** | Netlify | 정적 사이트 배포 + CI/CD |
| **PWA** | vite-plugin-pwa | 오프라인 지원, 홈 화면 설치 |
| **차트** | Recharts | 성장 추이 그래프 |
| **DnD** | @dnd-kit | 종목 순서 드래그 앤 드롭 |
| **아이콘** | Lucide React | UI 아이콘 |
| **날짜** | react-datepicker | 날짜 선택 UI |

---

## 3. 전체 구조

```
jumprope-competition-manager/
├── App.tsx                     # 메인 앱 (라우팅, 상태 관리)
├── index.tsx                   # 진입점
├── types.ts                    # TypeScript 타입 정의
├── constants.ts                # 기본 종목 데이터 (16개)
├── vite.config.ts              # Vite + PWA 설정
│
├── components/
│   ├── Sidebar.tsx             # 사이드바 (학년 선택)
│   ├── GradeView.tsx           # 학년별 메인 뷰 (탭 구조)
│   ├── LoginPage.tsx           # 로그인 페이지
│   ├── SettingsView.tsx        # 설정 페이지
│   │
│   ├── # 기록 입력 관련
│   ├── RecordInputModal.tsx    # 개인전 기록 입력 모달
│   ├── TeamManagementModal.tsx # 단체전 팀 관리 모달
│   ├── CompetitionTimer.tsx    # 경기 타이머
│   │
│   ├── # 참가자 선택
│   ├── ParticipantSelectionModal.tsx   # 단일 학급 참가자 선택
│   ├── MultiClassParticipantModal.tsx  # 다중 학급 참가자 선택
│   │
│   ├── # 팀 생성
│   ├── TeamCreationModal.tsx           # 단일 학급 팀 생성
│   ├── QuickTeamCreationModal.tsx      # 빠른 팀 생성
│   ├── MultiClassTeamCreationModal.tsx # 다중 학급 팀 생성
│   │
│   ├── # 결과 표시
│   ├── ScoreboardSummary.tsx           # 점수판 종합
│   ├── MatrixRecordTable.tsx           # 기록 매트릭스 테이블
│   ├── CompetitionRecordMatrixModal.tsx # 날짜별 기록 모달
│   │
│   ├── # 학급/학생 관리
│   ├── ClassManagementModal.tsx  # 학급 관리 모달
│   ├── CreateClassModal.tsx      # 학급 생성 모달
│   ├── StudentListModal.tsx      # 학생 목록 모달
│   ├── StudentRecordModal.tsx    # 학생 기록 상세 모달
│   │
│   ├── # 연습 모드
│   ├── PracticeModeView.tsx      # 연습 모드 뷰
│   ├── PracticeSessionModal.tsx  # 연습 세션 모달
│   ├── PracticeRecordChart.tsx   # 연습 기록 차트
│   │
│   ├── # 공통
│   ├── Button.tsx              # 공통 버튼 컴포넌트
│   ├── SortableEventCard.tsx   # 드래그 가능 종목 카드
│   ├── OfflineIndicator.tsx    # 오프라인 상태 표시
│   └── SessionNavBar.tsx       # 세션 네비게이션
│
├── contexts/
│   └── AuthContext.tsx         # Firebase 인증 컨텍스트
│
├── services/
│   └── firestore.ts            # Firestore CRUD 함수
│
├── lib/
│   ├── firebase.ts             # Firebase 초기화
│   └── statsCalculator.ts      # 통계 계산 유틸
│
├── utils/
│   ├── migration.ts            # 데이터 마이그레이션 유틸
│   ├── runMigration.ts         # 마이그레이션 실행 + 백업
│   └── offlineStatus.ts        # 오프라인 상태 관리
│
├── constants/
│   └── privacyPolicy.ts        # 개인정보처리방침
│
└── public/
    ├── manifest.json           # PWA 매니페스트
    └── icons/                  # PWA 아이콘
```

---

## 4. 전체 워크플로우 구조

### 4.1 교사 워크플로우

```
┌─────────────────────────────────────────────────────────────┐
│                        로그인                                │
│  (익명 인증 → userId 발급 → localStorage에 competitionId)    │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    학급 관리 (최초 1회)                       │
│  • 학급 생성 (예: 3학년 1반)                                 │
│  • 학생 등록 (이름 + 번호 입력)                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                 게임 설정 탭                                  │
│  ┌────────────────────────────────────────────────────┐     │
│  │ 1. 날짜 선택: [2025-11-27]                          │     │
│  │ 2. 모드 선택: [대회 ▾] / [연습 ▾]                   │     │
│  │ 3. 종목 선택: ☑️개인줄넘기 ☑️8자줄넘기 ☐짝줄넘기    │     │
│  │ 4. 참가자 설정: 종목별로 학생 선택                   │     │
│  │ 5. [게임 시작]                                      │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                 기록 입력 탭                                  │
│  ┌────────────────────────────────────────────────────┐     │
│  │ • 개인전: 학생별 점수 직접 입력 (±5 버튼)           │     │
│  │ • 단체전: 팀별 점수 입력                            │     │
│  │ • 타이머: 시간 제한 종목용                          │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                 결과 종합 탭                                  │
│  • 학급별 총점 순위                                          │
│  • 종목별 기록 매트릭스                                       │
│  • 날짜별 기록 조회                                          │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                 성장 추적 탭 (교사 주도 공유)                  │
│  • 학급 전체 학생 카드 목록 (성장률, 최고기록 요약)           │
│  • 학생 클릭 → 개인 성장 상세 페이지                         │
│  • 성장 차트 이미지/PDF 저장 → 학생/학부모에게 공유           │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 탭 구조 (리팩토링 후)

```
┌─────────────────────────────────────────────────────────────┐
│  게임 설정  │  기록 입력  │  결과 종합  │  성장 추적         │
│  (신규)     │  (기존)     │  (기존)     │  (신규)            │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 성장 추적 상세 (교사 주도)

```
┌─────────────────────────────────────────────────────────────┐
│ 성장 추적 탭                                                 │
├─────────────────────────────────────────────────────────────┤
│ 학급: [3학년 1반 ▾]  종목: [전체 ▾]                          │
├─────────────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                │
│ │ 김철수 │ │ 이영희 │ │ 박민수 │ │ 최수진 │ ← 학생 카드    │
│ │ 📈+15% │ │ 📈+8%  │ │ 📈+22% │ │ 📈+5%  │                │
│ │ 최고 52│ │ 최고 48│ │ 최고 61│ │ 최고 40│                │
│ └────────┘ └────────┘ └────────┘ └────────┘                │
├─────────────────────────────────────────────────────────────┤
│                         ↓ 학생 카드 클릭                     │
├─────────────────────────────────────────────────────────────┤
│ 🏃 김철수 성장 기록                          [이미지 저장]   │
├─────────────────────────────────────────────────────────────┤
│  [전체]  [대회]  [연습]  ← 탭 전환                           │
├─────────────────────────────────────────────────────────────┤
│  📊 성장 추이 차트 (● 대회 / ○ 연습)                        │
│  🏆 종목별 최고기록 (대회/연습 구분)                         │
│  📈 성장 요약 (총 연습 횟수, 성장률 등)                      │
└─────────────────────────────────────────────────────────────┘

**공유 방식**: 교사가 성장 차트를 이미지/PDF로 저장 후
             카카오톡, 학급 알림장 등으로 학생/학부모에게 전달
```

---

## 5. 데이터 구조

### 5.1 Firestore 컬렉션 구조

```
users/{userId}/
│
├── events/{eventId}              # 종목 마스터 (16개 기본)
│   └── {
│         id: 'event_1',
│         name: '개인줄넘기',
│         type: 'INDIVIDUAL' | 'PAIR' | 'TEAM',
│         defaultTimeLimit: 30,       # 초 (0 = 무제한)
│         defaultMaxParticipants: 6,
│         competitionId: 'comp_xxx'
│       }
│
├── classes/{classId}             # 학급 + 학생 정보
│   └── {
│         id: 'class_1',
│         grade: 3,
│         name: '3학년 1반',
│         competitionId: 'comp_xxx',
│         students: [{
│           id: 'student_1',
│           name: '김철수',
│           number: 1,
│           accessCode: 'AB3K'        # 학생 식별용 코드
│         }]
│       }
│
├── games/{gameId}                # 게임 세션 (신규)
│   └── {
│         id: 'game_xxx',
│         date: '2025-11-27',
│         mode: 'practice' | 'competition',
│         grade: 3,
│         eventIds: ['event_1', 'event_2'],
│         status: 'in_progress' | 'completed'
│       }
│
├── records/{recordId}            # 모든 기록 (핵심 컬렉션)
│   └── {
│         id: 'rec_xxx',
│         studentId: 'student_1',
│         studentName: '김철수',
│         accessCode: 'AB3K',         # 학생 조회용 코드
│         classId: 'class_1',
│         gameId: 'game_xxx',
│         eventId: 'event_1',
│         eventName: '개인줄넘기',
│         score: 52,
│         date: '2025-11-27',
│         mode: 'practice' | 'competition',
│         # 단체전 전용
│         teamId?: 'team_xxx',
│         teamMembers?: ['김철수', '이영희'],
│         teamScore?: 120
│       }
│
├── gradeConfigs/{compId_grade}   # 학년별 설정
│   └── {
│         grade: 3,
│         competitionId: 'comp_xxx',
│         events: {
│           [eventId]: {
│             selected: true,
│             targetParticipants: 6
│           }
│         }
│       }
│
└── accessCodes/{code}            # 코드 → 학생 매핑 (빠른 조회용)
    └── {
          studentId: 'student_1',
          studentName: '김철수',
          classId: 'class_1',
          className: '3학년 1반',
          grade: 3,
          userId: 'teacher_xxx'
        }
```

### 5.2 TypeScript 타입 정의

```typescript
// types.ts (주요 타입)

type EventType = 'INDIVIDUAL' | 'PAIR' | 'TEAM';
type RecordMode = 'competition' | 'practice';

interface Student {
  id: string;
  name: string;
  number?: number;
  accessCode: string;    // 학생 식별용 코드 (예: "AB3K")
}

interface CompetitionEvent {
  id: string;
  name: string;
  type: EventType;
  defaultTimeLimit: number;
  defaultMaxParticipants: number;
}

interface ClassTeam {
  id: string;
  grade: number;
  name: string;
  students: Student[];
  competitionId: string;
}

interface Game {
  id: string;
  date: string;
  mode: RecordMode;
  grade: number;
  eventIds: string[];
  status: 'in_progress' | 'completed';
}

interface Record {
  id: string;
  studentId: string;
  studentName: string;
  accessCode: string;
  classId: string;
  gameId: string;
  eventId: string;
  eventName: string;
  score: number;
  date: string;
  mode: RecordMode;
  // 단체전 전용
  teamId?: string;
  teamMembers?: string[];
  teamScore?: number;
}
```

### 5.3 접근 코드 생성 규칙

```typescript
// 혼동하기 쉬운 문자 제외: 0, O, I, l, 1
const CHARS = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789';

function generateAccessCode(): string {
  let code = '';
  for (let i = 0; i < 4; i++) {
    code += CHARS[Math.floor(Math.random() * CHARS.length)];
  }
  return code; // 예: "AB3K", "XY7P"
}
```

---

## 6. 직관적이고 효율적인 데이터 저장 세팅, 직관적인 UX

### 6.1 데이터 저장 최적화

| 원칙 | 구현 | 이점 |
|------|------|------|
| **학생 중심 저장** | `records` 컬렉션에 모든 기록 저장 | 성장 추적 시 단일 쿼리로 조회 |
| **코드 기반 식별** | accessCode를 record에 복사 | 학생별 기록 조회/공유 최적화 |
| **인덱스 활용** | studentId, accessCode, date 인덱스 | 빠른 쿼리 성능 |
| **mode 필드 통합** | ~연습/대회를 mode 필드로 구분 ~| 별도 컬렉션 불필요 |

**쿼리 예시**:
```typescript
// 학생 전체 기록 조회 (교사용)
firestore
  .collection('users').doc(userId)
  .collection('records')
  .where('studentId', '==', studentId)
  .orderBy('date', 'desc');

// accessCode로 학생 기록 조회 (성장 차트/공유용)
firestore
  .collectionGroup('records')
  .where('accessCode', '==', 'AB3K')
  .orderBy('date', 'desc');
```

### 6.2 UX 원칙

| 원칙 | 구현 |
|------|------|
| **최소 클릭** | 종목 카드 탭 → 즉시 기록 입력 모달 |
| **±5 버튼** | 점수 입력 시 스피너 대신 ±5 버튼 (빠른 입력) |
| **실시간 저장** | 입력 즉시 Firestore에 저장 (별도 저장 버튼 없음) |
| **오프라인 대응** | PWA + 오프라인 인디케이터로 현장 사용 최적화 |
| **반응형 UI** | iPad 세로/가로 모드 + 데스크톱 지원 |

### 6.3 네비게이션 구조

```
┌───────────────────────────────────────────────────────────────┐
│  사이드바                      │           메인 영역           │
│  ┌─────────────────────────┐   │  ┌─────────────────────────┐ │
│  │ 🏫 줄넘기 대회 관리       │   │  │ [탭 네비게이션]          │ │
│  │                         │   │  │ 게임설정│기록│결과│성장  │ │
│  │ 📚 학년 선택             │   │  ├─────────────────────────┤ │
│  │  • 1학년                 │   │  │                         │ │
│  │  • 2학년                 │   │  │  (선택된 탭 콘텐츠)      │ │
│  │  • 3학년 ← 선택됨        │   │  │                         │ │
│  │  • 4학년                 │   │  │                         │ │
│  │  • 5학년                 │   │  │                         │ │
│  │  • 6학년                 │   │  │                         │ │
│  │                         │   │  │                         │ │
│  │ ⚙️ 설정                  │   │  │                         │ │
│  │ 📋 학급 관리             │   │  │                         │ │
│  └─────────────────────────┘   │  └─────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
```

### 6.4 기록 입력 UI (핵심 인터랙션)

```
┌─────────────────────────────────────────────────────────────┐
│  개인줄넘기 기록 입력                              [X 닫기]  │
├─────────────────────────────────────────────────────────────┤
│  학생: 김철수 (1번)                                         │
│                                                             │
│  현재 점수: [ 52 ]                                          │
│                                                             │
│  [-5] [-1]  [직접 입력]  [+1] [+5]                          │
│                                                             │
│  ────────────────────────────────────────────────           │
│  이전 기록:                                                  │
│  • 11/20 (연습): 48회                                       │
│  • 11/15 (대회): 45회                                       │
│  • 11/10 (연습): 42회                                       │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 성장 차트 UI (대회/연습 구분)

```
┌─────────────────────────────────────────────────────────────┐
│  📊 개인줄넘기 성장 추이                                     │
│                                                             │
│  점수                                                        │
│  60┤                    ●───●                               │
│    │               ○──○         연습(실선)                  │
│  50┤          ●────●                                        │
│    │     ○────○              대회(점선)                     │
│  40┤○───●                                                   │
│    │                                                        │
│  30┤                                                        │
│    └────┬────┬────┬────┬────┬────→ 날짜                    │
│        11/1 11/8 11/15 11/22 11/29                          │
│                                                             │
│  ── 연습 (○)    ●● 대회 (●)                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 개발 명령어

```bash
npm run dev      # 개발 서버 시작 (http://localhost:5173)
npm run build    # 프로덕션 빌드 (dist/)
npm run preview  # 빌드 결과 미리보기
```

**빌드 성공 시**: `npm run dev`로 개발 서버를 띄워 변경사항을 확인하세요.

## 배포

**Netlify만 사용** (Firebase Hosting 사용 금지)

```bash
# 배포 방법
git push origin main    # GitHub push → Netlify 자동 배포

# 또는 수동 배포
npx netlify-cli deploy --prod
```

- **자동 배포**: main 브랜치 push 시 Netlify가 자동 빌드/배포
- **미리보기**: PR 생성 시 Preview URL 자동 생성
- **백엔드**: Firebase (Firestore, Auth)는 그대로 사용
- **호스팅**: Netlify만 사용 (Firebase Hosting X)

---

## 주요 파일 참조

- **메인 앱**: `App.tsx`
- **타입 정의**: `types.ts`
- **Firestore 서비스**: `services/firestore.ts`
- **기본 종목 데이터**: `constants.ts`
- **리팩토링 계획**: `REFACTORING_PRD.md`

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 임시: 마이그레이션을 위해 모든 읽기/쓰기 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
